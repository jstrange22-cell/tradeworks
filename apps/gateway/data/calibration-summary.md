## CALIBRATION
MEMORY_DB_URL is unset. Calibration will be empty until the memory DB is configured.

## CALIBRATION (last 365d, 0 trades)

No closed approves yet — calibration will activate once outcomes are recorded.